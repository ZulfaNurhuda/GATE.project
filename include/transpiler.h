#ifndef TRANSPILER_H
#define TRANSPILER_H

#include <string>

// This is a dummy function representing the transpiler's core functionality.
// It takes a string of NOTAL code and returns the transpiled Pascal code.
std::string transpile(const std::string& notal_code);

#endif // TRANSPILER_H
